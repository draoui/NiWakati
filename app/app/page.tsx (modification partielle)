      {/* Quick Access Section */}
      <section className="bg-primary/5 py-12">
        <div className="container mx-auto px-4">
          <div className="grid gap-6 md:grid-cols-3">
            <Card className="p-6">
              <h3 className="mb-4 flex items-center text-lg font-semibold">
                <Users className="mr-2 h-5 w-5" />
                Je suis réfugié(e)
              </h3>
              <p className="mb-4 text-muted-foreground">
                Découvrez nos cours de français gratuits et nos activités d'intégration
              </p>
              <Button variant="outline" asChild className="w-full">
                <Link href="/calendar">Voir les activités</Link>
              </Button>
            </Card>
            {/* ... autres cards ... */}
          </div>
        </div>
      </section>

      {/* Activities Section */}
      <section className="section-padding">
        <div className="container mx-auto px-4">
          <h2 className="mb-12 text-center text-3xl font-bold">Nos Activités</h2>
          <div className="grid gap-8 md:grid-cols-3">
            {activities.map((activity, index) => (
              <Card key={index} className="overflow-hidden">
                {/* ... contenu de la carte ... */}
                <Button variant="outline" asChild className="w-full">
                  <Link href="/calendar">En savoir plus</Link>
                </Button>
              </Card>
            ))}
          </div>
        </div>
      </section>